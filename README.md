# Groonga C API sample
## Compile
   % gcc -Wall -O2 -lgroonga -I/usr/include/groonga ctx_sample.c -o ctx_sample.o  
   % gcc -Wall -O2 -lgroonga -I/usr/include/groonga c/table_sample.c -o table_sample.o

   or

   % make
